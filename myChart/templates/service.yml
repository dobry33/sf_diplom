apiVersion: v1
kind: Service
metadata:
  name: tutorial-service
  labels:
    app: django-pg-docker-tutorial
    
spec:
  type: ClusterIP
  ports:
    - port: {{values.app.containerPort}}
      targetPort: {{values.app.hostPort}}
      protocol: TCP

  selector:
    app: django-pg-docker-tutorial
   